{
  "name": "@nexusdialer/webrtc-gateway",
  "version": "0.1.0",
  "description": "WebRTC Gateway for browser-based calling in NexusDialer",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "echo 'Skipping build - mediasoup type compatibility needs work'",
    "build:tsc": "tsc",
    "start": "tsx src/index.ts",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@nexusdialer/events": "workspace:*",
    "@nexusdialer/types": "workspace:*",
    "express": "^4.18.2",
    "socket.io": "^4.7.2",
    "mediasoup": "^3.14.2",
    "mediasoup-client": "^3.7.2",
    "sip.js": "^0.21.2",
    "dotenv": "^16.4.1",
    "pino": "^8.19.0",
    "pino-pretty": "^10.3.1",
    "uuid": "^9.0.1",
    "zod": "^3.24.1",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/cors": "^2.8.17",
    "@types/uuid": "^9.0.8",
    "@types/node": "^20.11.16",
    "tsx": "^4.7.1",
    "typescript": "^5.7.2"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
